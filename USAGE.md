# 保衛羊村自動化工具 - 詳細操作與使用手冊

本手冊針對 `sheep-village-automation` 專案中的三大核心工具提供完整的介面說明、快捷鍵配置、取點教學與操作步驟。

---

## 快速導覽

1. [功能一：敲狼加速礦 (wolf_mine_speedup.py)](#功能一敲狼加速礦-wolf_mine_speeduppy)
2. [功能二：BOSS 自動連點 (boss_auto_clicker.py)](#功能二boss-自動連點-boss_auto_clickerpy)
3. [功能三：防禦塔升級與攻擊 (upgrade_and_attack.py)](#功能三防禦塔升級與攻擊-upgrade_and_attackpy)
4. [全域緊急停止與常用熱鍵對照表](#全域緊急停止與常用熱鍵對照表)

---

## 功能一：敲狼加速礦 (`wolf_mine_speedup.py`)

### 核心特色
- **草地網格覆蓋盲敲**：採用熱點座標多點高速連點，即使間諜狼縮在木桶中未開蓋，也能 100% 敲中。
- **好友自動巡邏與翻頁**：循序進入好友家園執行敲狼與挖礦加速。
- **邀請彈窗智慧終止**：當翻頁到達好友名單末端、點到空位跳出「暫時不支持邀請好友」彈窗時，自動判定巡邏完畢並返回自家家園。
- **設定全自動記憶**：所有點位、草地網格與參數均會自動存入 `config.json`，下次啟動無須重複設定。

### 啟動方式
- 直接雙擊 `start_wolf_mine.bat`，或在終端機輸入：
  ```bash
  python wolf_mine_speedup.py
  ```

### 操作步驟教學

#### 步驟 1：設定好友座標
- **方式 A（兩點生成好友列表）**：
  1. 將滑鼠移至好友欄「第 1 位好友頭像」上方，按鍵盤 **`J`** 鍵記錄點 1。
  2. 將滑鼠移至好友欄「第 5 位（或最後一位）好友頭像」上方，按鍵盤 **`J`** 鍵記錄點 2。
  3. 點擊介面上的「兩點生成好友座標」，系統將自動等距計算整排好友頭像座標。
- **方式 B（逐點手動新增）**：
  - 將滑鼠移至各好友頭像上，依序按鍵盤 **`J`** 鍵即可逐一加入列表。

#### 步驟 2：設定敲狼草地熱點
- **方式 A（框選草地生成網格）**：
  1. 將滑鼠移至家園草地左上角，按鍵盤 **`K`** 鍵記錄頂點 1。
  2. 將滑鼠移至家園草地右下角，按鍵盤 **`K`** 鍵記錄頂點 2。
  3. 點擊「兩點生成敲狼網格」，程式會自動在草地範圍內生成均勻密集的盲敲點。
- **方式 B（手動新增熱點）**：
  - 滑鼠移至常出狼的位置，按 **`K`** 鍵手動標記。

#### 步驟 3：設定輔助座標（選填）
- **自己家園圖示座標**：滑鼠移至遊戲畫面右上角/左下角的「自家領地/回城」按鈕，點擊介面「設置家園座標」。
- **礦山固定座標**：若沙漏偶爾辨識不到，可將滑鼠移至礦區位置，點擊介面「設置礦區座標」做為兜底。

#### 步驟 4：調整參數並啟動
- **加載等待 (秒)**：進入好友家園時等待場景載入的時間（預設 1.8 秒）。
- **點擊間隔 (秒)**：每個敲狼點之間的敲擊速度（預設 0.12 秒）。
- **敲狼後等待 (秒)**：敲擊完整片草地後等待獎勵彈窗的時間（預設 0.6 秒）。
- **最大頁數**：好友欄翻頁上限。
- **勾選項目**：確認勾選「自動敲狼」、「挖礦加速」、「自動翻頁」、「點到邀請好友彈窗時自動停止並返回家園」。
- 點擊 **【開始巡邏】** 即可自動全自動作業。

---

## 功能二：BOSS 自動連點 (`boss_auto_clicker.py`)

### 核心特色
- **自動辨識 BOSS 與驅趕小怪**：以影像辨識自動鎖定世界 BOSS（`boss.png`）、4 階 BOSS（`4boss.png`）並點擊攻擊（`gongji.png`）。
- **小怪自動跳過/關閉**：辨識一般、普通、困難等小怪彈窗並點擊關閉，專注擊殺 BOSS。
- **支援視窗置頂與定時循環**。

### 啟動方式
- 直接雙擊 `start_boss_clicker.bat`，或在終端機輸入：
  ```bash
  python boss_auto_clicker.py
  ```

### 操作步驟教學

1. **開啟權限與視窗**：
   - 建議點擊介面上的「以管理員身份運行」，確保滑鼠點擊不會被瀏覽器安全性攔截。
   - 勾選「視窗置頂」，方便監控日誌輸出。
2. **調整參數**：
   - **循環間隔 (分鐘)**：每輪掃描與攻擊 BOSS 後休息的時間（例如設為 5 或 10 分鐘）。
   - **匹配閾值**：預設為 `0.90`。若畫面解析度微偏導致無法偵測到 BOSS，可將拉桿調降至 `0.80` ~ `0.85`。
3. **啟動點擊**：
   - 點擊 **【開始運行】**，腳本將在背景全自動巡檢螢幕、點擊 BOSS 並攻擊。

---

## 功能三：防禦塔升級與攻擊 (`upgrade_and_attack.py`)

### 核心特色
- **自動巡檢防禦塔**：依序巡邏多個防禦塔座標。
- **空閒苦工自動調配**：自動辨識未工作的空閒力量圖示（`liliang.png`），排除工作中標誌，自動點擊加號並確認升級。
- **定時支援攻擊模式**。

### 啟動方式
- 直接雙擊 `start_upgrade_attack.bat`，或在終端機輸入：
  ```bash
  python upgrade_and_attack.py
  ```

### 操作步驟教學

1. **添加防禦塔座標**：
   - 將遊戲畫面切換至防禦塔所在場景。
   - 將滑鼠移至防禦塔上方，按下鍵盤 **`J`** 鍵記錄座標（可記錄多座防禦塔）。
2. **設定時間與次數**：
   - **點擊間隔 (秒)**：點擊防禦塔後的反應延遲（預設 1.0 秒）。
   - **連點次數**：每座防禦塔嘗試升級苦工的連點次數。
   - **循環間隔 (秒)**：整輪巡檢結束後的冷卻時間。
3. **啟動自動化**：
   - 點擊 **【開始升塔】**：自動執行開塔、辨識力量、分配苦工、升級與關閉視窗。
   - 點擊 **【開始攻擊】**：定時點擊攻擊按鈕進行輔助輸出。

---

## 全域緊急停止與常用熱鍵對照表

| 快捷鍵 | 作用程式 | 功能說明 |
| :--- | :--- | :--- |
| **`F10`** | 全工具共用 | **全域緊急停止**：任何時候按下此鍵，立即中斷所有自動化腳本運作。 |
| **`J`** | `wolf_mine_speedup.py` | 標記目前滑鼠所在位置為「好友頭像座標」。 |
| **`J`** | `upgrade_and_attack.py` | 標記目前滑鼠所在位置為「防禦塔座標」。 |
| **`K`** | `wolf_mine_speedup.py` | 標記目前滑鼠所在位置為「草地敲狼熱點」。 |

---

## 常見問題與排查

- **Q1: 腳本啟動後滑鼠沒有反應或點偏？**
  - 請檢查 Windows 系統縮放設定是否為 **100%**。
  - 建議以系統管理員身分執行批次檔。
- **Q2: 圖像一直顯示「未在螢幕上看到」？**
  - 請確認遊戲畫面未被其他視窗完全遮擋。
  - 可於介面上將「匹配閾值（Threshold）」由 0.90 微調至 0.80。
